# DX棚卸アプリ 引き継ぎ資料

最終更新: 2026-08-26 / 現行バージョン: **Code.gs v4.0.0 / index.html v4.1.0**

---

## 1. プロジェクト概要

カクイチ（一鍬商事）の製造業DX案件。工場の棚卸をスマホ（iPhone想定）で行うPWA的Webアプリ。

**構成**
```
index.html (GitHub Pages) ──→ GAS (Code.gs) ──→ スプレッドシート ──→ ASTERIA ──→ IBM i
   IndexedDB(履歴+マスタ)        CacheService        (棚卸データ)
```

- リポジトリ: `itajima-kz/inventory-qr3`
- スプレッドシートID: `1VWwIHfA-75pI3UuTABcyysABKUmnk6CZ95khTxkL344`
- 読取方式: JAN(ZXing) / QR(jsQR) / OCR(Google Cloud Vision API 経由GAS)
- オフライン前提。スキャンはIndexedDBに貯め、履歴タブから一括送信。

**担当者の役割分担（重要）**
- 田嶋さん: 質問・相談・提案依頼、意見の相違は率直に投げる、**最終判断**
- AI側: 世界中のデータを元に最善の回答・提案、必要に応じ追加提案、立場を考慮

→ 実装前に選択肢とトレードオフを示し、判断を仰ぐ進め方が定着している。勝手に大きな設計変更をしない。

---

## 2. スプレッドシートの実構造（**コード内コメントより実測を信じること**）

### プルダウンマスタ（実測で確認済み）

| 列 | ヘッダー | 用途 |
|---|---|---|
| A | 棚卸部門 | getDropdownsが返すが**UIの場所選択には未使用** |
| B | 部門コード | アプリ未使用 |
| C | 担当者部門 | **担当者マップのキー。UIの場所ドロップダウンもここ由来** |
| D | 担当者 | 担当者名 |
| E | 担当者コード | アプリ未使用 |
| F | 在庫区分 | 製品 / 仕掛品 / 材料 |

実測: A列16部門・C列distinct16部門で**完全一致**、全部門に担当者1名以上。
v3.5.0でフロントを `Object.keys(persMap)`（=C列由来）に統一し、A/C列の食い違いを構造的に排除済み。

### 製品マスター最新（実測）

| 列 | ヘッダー |
|---|---|
| A | 製品コード |
| B | 製品名 |
| C | 品名・規格・サイズ（サイズ） |
| D | 長さ（未使用） |
| E | 数量（未使用） |
| F | 単位 |

- **70,200件**（ユニーク。重複1件）
- **品名・サイズが全角スペース(U+3000)で固定長パディング**されている（IBM iの名残）。UTF-8で1文字3バイトのため肥大化の主因だった。
- サイズ欄は40,573件がパディングのみ（トリムすると空）。

### その他シート
- 棚卸データ: A=日付,B=時刻,C=図番,D=製番,E=品名,F=サイズ,**G=長さ**,H=数量,I=棚卸部署,J=担当者,K=在庫区分
  - **v4.0.0で G列「長さ」を追加（10列→11列）**。GAS側は `INV_HEADER` / `INV_COLS` 定数を使い、列位置を直書きしない。
- 棚卸データ(特注品): 棚卸データと同じ11列。マスタ未登録かつ図番がK/P始まりのものをここへ分離（v4.0.0）。無ければGASが自動生成する。
- 末尾0リスト: A=製品コード（末尾0が正規なもの）
- 送信ログ: A=受信日時,B=場所,C=担当者,D=送信件数,E=新規,F=更新,G=エラー,H=スキャン日時範囲（列は変更なし。特注品分も合算）

---

## 3. 末尾0処理の仕様（**v4.1.0で改訂**）

`stripTrailingZeroSpace()` の仕様。空白除去・ハイフン除去・大文字化した後、

1. 末尾が0か確認
2. **その時点の文字列そのもの**が
   - ① 末尾0リストにある … または … ② **製品マスターに実在する**
   ならそこで確定（正規コード）
3. どちらにも無ければ末尾0を1文字削って 1 に戻る
4. 末尾が0でなくなるまで繰り返す
5. **削り切っても①②のどちらにも該当しなかったら、削らずに読み取った原文を返す**

= 「フルの形から順に、リストに載っている形かマスタに実在する形が最初に見つかった所で止める。
　　どこにも見つからなければ**何も削らない**」

**呼ばれる場所**: `scanQR()` / `scanJAN()` のみ。OCR・手入力には適用されない。

### v4.1.0 での変更点（田嶋さん指示）
旧仕様は、末尾0リストに載っていなければ**末尾0を全部削って**返していた。
そのため末尾0で終わる正規コードがリストに登録漏れしていると、実在しないコードに化けていた
（旧 §3 に「急所」として書いていた弱点そのもの）。

- 判定材料に**製品マスターを追加**。リストの登録漏れをマスタが補う。
- 削り切っても該当が無い場合は**除去処理そのものを行わない**。
  どのみち「マスタ未登録」として手入力に回る場面なので、切り詰めた推測値より
  ラベルに印字された原文が残っている方が現場で判断できる。

**マスタ未読込時のフォールバック**: `masterData` が空のとき（オフライン起動直後・マスタ取得失敗時）は
判定材料が無い。ここでマスタ未ヒット扱いにすると**一切削らなくなってしまう**ため、
その場合だけ旧仕様（末尾0リストのみで判定）で動く。

**残る運用前提**: 末尾0リスト（updateTailZeroList.gsでマスタA列から自動生成）は引き続き有効。
マスタに無いがリストにはある、という形を拾うため。ただし登録漏れの影響はv4.1.0で大きく下がった。

### 検証
`scratchpad/test_tailzero.js` が index.html から関数本体を抜き出してNodeで実行する。
スタブ（`masterData` / `tailZeroSet` / `findMasterByCode`）はアプリと同じ形。12ケース全通過。
主要ケース:

| 入力 | マスタ | 結果 |
|---|---|---|
| `AB123400` | `AB1234` あり | `AB1234`（削る） |
| `CD50000` | `CD500` あり | `CD500`（途中で止まる） |
| `GH12000` | `GH12000` あり | `GH12000`（**旧実装では `GH12` に化けていた**） |
| `ZZ99900` | 該当なし | `ZZ99900`（**削らない＝v4.1.0の主旨**） |
| `AB123400` | リストに `AB12340` | `AB12340`（リストが優先） |

---

## 4. これまでに修正した内容（v3.4.0 → v4.1.0）

### v4.1.0（index.htmlのみ。**Code.gsは変更なし＝v4.0.0のGAS更新以外に再デプロイ不要**）
- **末尾0除去の判定に製品マスターを追加し、どこにも該当が無ければ削らないようにした**（田嶋さん指示）。
  仕様の詳細・変更理由・テストケースは **§3** に全文記載。
- 併せて `stripTrailingZeroSpace()` のコメントを改訂版に差し替え。

### v4.0.0（Code.gs / index.html 両方。**GASの再デプロイが必要**）

田嶋さんからの依頼①〜⑤を実装。⑥は案出しのみで実装は保留（下記 §6）。

1. **① 表記変更「JANコード」→「バーコード」**
   表示文言のみ。内部識別子（`mode==='jan'` / `janReader` / `scanJAN`）は据え置き。
   モードボタンは文字数が増えたので font-size 16px→13px。

2. **② 同一図番の重複登録を許可**
   複合キーを `図番＋製番＋部署＋担当者` → **`日付＋時刻＋図番＋製番＋部署＋担当者`** に変更。
   単に「常にinsert」にすると `update_item` が対象行を見失うため、スキャン時刻（秒精度＝実質一意）を
   キーに加えることで以下3つを同時に満たした:
   - 別々にスキャンした同一図番 → 別行として追加（＝②の要件）
   - 履歴修正 → 対象行を正しく特定（日付・時刻は修正で変わらない）
   - 送信失敗後の再送 → 同じ行を上書きするので重複行が出ない
   **急所**: A/B列は `getValues()` で **Date が返ることがある**（Google側が日付値へ自動変換するため）。
   そのままだと端末の `'2026/08/26'` と一致せず全件が「新規」判定になり重複行を量産する。
   → `normKeyDate()` / `normKeyTime()` でスプレッドシートのタイムゾーン基準に書式を揃えてから比較。
   A/B列を文字列書式(@)にする案は下流のASTERIAの解釈を変える恐れがあるため**採らなかった**。

3. **③ 長さ(mm)入力**
   品名の先頭が対象部材14種（Ｈ型鋼/ＬＨ鋼/Ｃ型鋼/表面処理　Ｃ型鋼/角パイプ/平鋼/古河フォームエース/
   古河　フネンエース/古河タイコウエース/表面処理鋼板/ＺＡＭ/ＭＡＧ/ステンレスＳＵＳ３０４/ガルバリュウム）
   に完全一致したときだけ、登録モーダルと修正モーダルに長さ欄を出す。mm整数・数字のみ。
   **急所**: v3.7.0の `normText()` が全角スペースを半角1個に畳んでいるため、`'表面処理　Ｃ型鋼'` は
   端末には `'表面処理 Ｃ型鋼'` として届く。フロントの `normNameForMatch()` で**同じ正規化を判定リスト側にも
   かけて**から前方一致させている。全角/半角英数の変換はしていないので、マスタに `H型鋼`（半角H）が
   混ざっていると一致しない点は要確認。
   **長さ欄が出た場合、長さの入力は必須**（田嶋さん判断）。未入力・0では登録できない。
   - `isValidLen()` を判定の唯一の入口にし、`updReg()`（登録ボタン活性）/ `updEmSaveBtn()`（保存ボタン活性）/
     `regData()` / `saveEditModal()` の4箇所で同じ関数を使う。
   - **急所**: 数量欄のEnterキーは `regData()` を直接呼ぶのでボタンの `disabled` を経由しない。
     ボタン活性だけの制御では素通りするため、`regData()` / `saveEditModal()` 側にも同じ判定を置いている。
   - 警告は入力欄下に出しっぱなしのメッセージ（`#regLenMsg` / `#emLenMsg`）＋下線オレンジ＋登録時トーストの3段構え。
   - 修正モーダル側も必須にした。ここを緩めると「登録時は必須なのに修正で空にできる」抜け道になり、
     結局シートに長さ空欄の対象部材が入ってしまうため。**v4.0.0以前の対象部材レコードを履歴から開くと
     長さの入力を求められる**（＝旧データの穴埋めが促される）。キャンセルは可能。
   - マスタに半角表記（`H型鋼`）の登録は無いことを確認済み（田嶋さん）。全角半角変換は入れていない。

5. **⑤' 登録モーダルの圧縮とメッセージ規約**（長さ行が増えて縦に伸びた対応）
   - **モーダル内のメッセージは、接頭の絵文字を除き全角25文字以内・1行厳守**（田嶋さん指示）。
     `.reg-unreg-note` / `.reg-custom-note` は `white-space:nowrap;overflow:hidden;text-overflow:ellipsis` で
     構造的に2行にならないようにしてある。文言を足すときはこの制約を壊さないこと。
     現行: 「マスタ未登録です。図番と品名を入力」(17字) / 「棚卸データ(特注品)シートへ登録されます」(20字) /
     「長さの入力は必須です（半角数字）」(16字)
   - 余白・フォントサイズを全体的に詰めて **612px → 503px（-109px）** に圧縮（実測値。375x812で計測）。
   - **登録ボタン行を `position:sticky; bottom:0` に変更**。高さを削ってもソフトキーボードが出れば下端は隠れるため、
     根本対策としてスクロール領域の下端に貼り付けた。実測で 375x667 の最悪ケース
     （未登録＋特注＋長さ欄あり = 634px）でもボタンは常時画面内に出る。
   - 実測手順: `python -m http.server` で配信 → ブラウザで `applyCode()` を直接叩いてモーダルを開き、
     `.reg-modal-box` の `scrollHeight` / `clientHeight` を測る。
     **注意**: バックグラウンドタブでは `slideUp` アニメーションが0%で止まり、`getBoundingClientRect()` が
     `translateY(100%)` のままの値を返す。計測前に `box.style.animation='none'` を入れること。

4. **④ 特注品の分離**
   マスタ照合で品名が得られず、**かつ**図番がK/Pで始まるものを `棚卸データ(特注品)` シートへ。
   判定はフロント（`isCustomRecord()`）で行い `items[].isCustom` としてGASへ渡す。
   GASで再照合しないのは、フロントは全マスタをメモリに持っていて照合コストがゼロなのに対し、
   GAS側でやるとシート70,200行の読込が毎回増えるだけだから。
   `update_item` は図番修正で通常品⇔特注品が入れ替わったときの**シート間移動**にも対応（`action:'moved'`）。

5. **⑤ QR読取範囲をガイド枠内に限定**
   **これが今回の最大の発見**: 旧実装のコーナー枠は cam-area の四隅から7pxに置かれた**ただの飾り**で、
   `scanQR()` は `jsQR` に**フレーム全体**を渡していた。つまり枠はデコード範囲を一切制限しておらず、
   枠のCSSを縮めても症状は1ミリも変わらない。製造指示書のようにQRが並ぶ紙面で隣のQRを拾うのは当然だった。
   → `qrGuideBox()` が枠の位置・サイズを1箇所で決め、表示(`updQrGuide`)とデコード(`getQrRoi`)の
     両方がそれを参照する構造に変更。`QR_ROI_RATIO = 0.60`（表示エリア短辺に対する比率）1箇所で調整可能。
   → バーコードは横長で狭めると逆に読めなくなるためROIを掛けず全画面のまま。
   → 表示座標→映像座標の変換（object-fit:coverのはみ出し＋デジタルズーム補正）は
     `mapGuideToVideo()` に集約し、`capFrame()`（OCR）も同じ関数を通すようにした。片方だけ壊れなくなる。

6. **付随: `findMasterByCode()` をMap索引化**
   旧実装は呼ばれるたびに masterData 70,200件をリニア走査していた。③で修正モーダルの図番入力
   1文字ごとに引く必要が出たため、`masterIndex`（正規化キー→レコードのMap）を遅延生成する方式に変更。
   重複コードは先勝ちで旧実装と同じ結果になる。`applyMasterData()` で索引を無効化して作り直させる。

7. **付随バグ修正**: `updateItem` の `newItem.hinmei || oldItem.hinmei` は、図番変更でマスタ未登録になり
   品名を意図的にクリアした場合でも**旧品名が復活していた**。`pick()` で「空文字は新しい値として尊重」に修正。

### v3.4.0 → v3.8.0（履歴）

### v3.5.0（Code.gs）
1. **registerBatch 重複キーで例外**（最優先バグだった）
   同一バッチ内で複合キー（図番+製番+部署+担当者）が重複すると、2件目を負の行番号で updates に積み `getRange(-1)` が例外 → **その回の全updatesを巻き添え失敗**させていた。
   → `keyMap[key]` を3分岐（undefined=新規insert / 正=既存行更新 / 負=バッチ内先行insertをlast-write-winsで上書き）に変更。
   → updates の書き込みを1行ごとtry-catchに変更（1行の失敗が他を巻き込まない）。
   発火条件: 製番が空のJAN/QRで同じ図番を2回スキャン。「たまに送信エラー」の正体。

2. 場所ドロップダウンを persMap キー由来に統一（フロント `applyMasterData`）
3. プルダウンマスタの列定義コメントを実シートに合わせ訂正

### v3.6.0
1. **[Code.gs] キャッシュのバイト数未考慮**
   旧 `if (str.length < 90000) cache.put(...)` は**文字数**判定。CacheServiceの上限は**100KB(バイト)**。日本語は3バイトなのですり抜けて例外→GETごと落ち、フロントは masterData が空になり、その壊れた状態をキャッシュしていた。
   → `safeCachePut()` / `safeCacheGet()` に集約。`Utilities.newBlob().getBytes().length` で実バイト判定＋try-catch。上限超過時はキャッシュを諦めるだけでデータは正しく返す。
   → diagTest にキャッシュサイズ診断（バイト数）追加。

2. **[index] 履歴修正の二重書き込み**
   `saveEditModal` が未送信レコードでも `update_item` を呼び、GAS側は該当行なしで inserted_fallback → その後の一括送信でも同キーが送られ**重複行**のリスク。
   → `wasSent = (oldItem.sent_flag === 1)` を先に退避し、送信済みだった場合のみ update_item を呼ぶ。未送信は一括送信に任せる。

3. **[index] デジタルズームがスキャン画像に効いていない**
   HWズーム非対応端末では video に CSS `scale()` をかけるだけで、`drawImage(v,0,0,w,h)` はネイティブ全画面フレームを取得 → **拡大しても認識率が変わらなかった**。
   → `drawScanFrame()` を新設。デジタルズーム中は中央 1/z 領域を切り出してキャンバス全面に引き伸ばす。scanQR/scanJAN両方で使用。
   → `capFrame()`（OCR）にも座標補正を追加（CSS scaleは中心基点なのでガイド枠座標を中心方向へ1/zに縮める）。
   → 副次バグ: `stopCam()` が transform をリセットしておらず「×1.0表示なのに映像だけ拡大」状態が次回に持ち越されていた → 停止時・能力初期化時の両方でリセット。
   → 倍率表示に `(D)` を付けデジタル（画質劣化あり）を明示。

4. **[index] 図番変更時に品名が古いまま**
   `_emHitData`（「検索」ボタンを押した時のみ入る）がnullだと旧図番の品名・サイズが新図番に付いたまま保存されていた。
   → 図番が変わったら保存時に必ず `findMasterByCode()` で再照合。未登録なら品名・サイズをクリア（誤った組合せを残さない）。
   → マスタ照合を `findMasterByCode()` に集約（3箇所に散っていた）。
   → `emSearchMaster` が `hit.hinmei` を先に見ていたが正しくは `product_name`。修正済み。

### v3.7.0
1. **[Code.gs] 案A: パディング除去**
   `normText()` を新設し getMasterMap の品名・サイズ・単位に適用（全角スペース→半角、連続空白畳み込み、トリム）。**図番には適用しない**。
   → master_v3 10.54MB → 約7.18MB（-32%）。サイズ欄の表示・検索精度も向上。

2. **[index] 案B: マスタキャッシュを localStorage → IndexedDB**
   **これが最大の発見**: マスタは数MBあり localStorage(5MB上限)に入らず、旧 `lsSave` は QuotaExceededError を**空のcatchで握りつぶしていた**。つまりオフライン復元／GAS失敗フォールバックが**一度も機能していなかった**（TTLだけは保存されるので isCacheValid は true を返していた）。
   → IndexedDBを v2 に上げ `meta` ストア追加。`metaGet`/`metaPut` 新設。`saveMasterCache`/`loadMasterFromCache` を非同期IndexedDB版に。呼び出し3箇所を await 化。
   → 旧localStorageキーを掃除する `purgeLegacyLsCache()` 追加。
   → 設定タブに「キャッシュ状態を確認」ボタン（`checkCacheStatus()`）を追加し、保存件数・サイズ・保存日時を可視化。

### v3.8.0（index.htmlのみ。**Code.gsは変更なし＝再デプロイ不要**）
**キャッシュ優先（TTL活用）**
旧実装はキャッシュがあってもオフライン時とGAS失敗時にしか使わず、通常起動では毎回数MBを再ダウンロードしていた。
→ `loadAllMaster` 冒頭でキャッシュを先に確認。3分岐:
- 24時間以内 → **通信せず即起動**（トーストに経過時間表示）
- 24時間超＋オンライン → 従来どおり通信して取り直し
- 24時間超＋オフライン → 使う（「保存済みマスタで起動（38時間前）」と明示）

`isReload=true`（再読込ボタン）はTTLを無視して必ず取得。マスタタブにも「保存済みデータを使用（3時間前）— 最新にするには「再読込」」を表示。

**運用上の注意**: マスタ更新当日、既存キャッシュ端末には24時間反映されない。「再読込」で即反映可。設定タブ使い方⑧に太字で明記済み。

---

## 5. 実測データ（判断の根拠。再測定不要）

| 対象 | サイズ |
|---|---|
| master_v3（パディング有・修正前） | 10.54MB（CacheService上限95KBの**111倍**） |
| master_v3（トリム後・現行） | 約7.18MB（依然キャッシュ不可） |
| getMasterList（端末転送・修正前） | 12.14MB |
| getMasterList（トリム後・現行） | 約8.79MB |
| 短縮キー＋空欄省略 | 6.57MB |
| 同 gzip | **0.47MB** |

- 製品コード 70,200件。棚卸データ実績で使われたユニーク図番は **626件（全体の0.89%）**、うちマスタ存在600・未登録26。
  → ただし「どの図番が読まれるか事前確定不可」なので、使う分だけ配る方式は原則採れない。
- diagTest実測ログ: all_personnel_v1=1,670bytes / dropdowns_v3=270bytes（両方キャッシュ可）。master_v3のみ上限超過。

---

## 6. 未対応・次の検討事項

### ⑥ 送信し忘れ防止（**案出しのみ。実装は未着手・別途判断**）

「送信を忘れる」のは*人が送信ボタンを押す運用*だから起きるので、送信を意識させない方向が本命。

#### 6-0. 確定した前提条件（2026-08-26 田嶋さん回答）

| 項目 | 回答 | 案への影響 |
|---|---|---|
| 工場内のWiFi電波 | **届かない区画がある** | **A・Fの効果が落ちる**（圏外では送信自体が失敗する）。人に促す B と、確実に届く G の重要度が上がる |
| 現場iPhoneのiOS | **最低 iOS 18。近々 iOS 26 へ** | **案H（アイコンバッジ）が実用圏に入った**（要件は16.4以降）。`fetch(keepalive)` も安定して使える |

**未確認の重要点**: 端末は**WiFi専用か、セルラー（SIM）も入っているか**。
セルラーがあれば圏外区画でもLTEで送信できるため、Aの効果は落ちない。
WiFi専用なら圏外区画＝完全オフラインとなり、下記の設計が必須になる。

#### 6-1. 案の比較（A〜I）

| 案 | 内容 | 効果 | 懸念事項 | 実装コスト |
|---|---|---|---|---|
| **A. 自動送信** | オンライン時に ①一定件数（例10件）たまる ②5分ごと ③オンライン復帰時 のいずれかで自動一括送信 | 高（**圏外区画があるため「最大」からは後退**）。圏内にいる間は忘れる余地が消える | **圏外では働かない**／`navigator.onLine` はAPに繋がっていれば疎通不能でも `true` を返すため、`online`イベントだけでは不十分で**定期リトライが必須**／誤データもすぐシートに入る（②の重複登録＋修正運用で許容可）／送信の重なりを防ぐ排他制御が要る／通信量とバッテリー | 中 |
| **B. 場所/担当者の変更時に確認** | `onDept()`/`onPers()` で値が変わったとき、未送信があれば送信確認ダイアログ | 高。「その場の棚卸が終わった」という明確なサインを捉えられる | 選び直し（誤操作の訂正）でも出るので煩わしい／**同一の場所・担当者で一日中作業する場合は一度も発火しない** | 小 |
| **C. 経過時間アラート** | 最古の未送信が◯時間を超えていたら、起動時＋定期的に警告モーダル | 中 | 閾値の決め方が難しい／作業中に割り込むと邪魔／モーダルを連発すると無視されるようになる | 小 |
| ~~**D. アプリを離れる時の警告**~~ | ~~`visibilitychange` で未送信件数を警告表示~~ | **なし（不採用）** | **発火した時点でアプリは既に非表示。警告を出しても誰も見ない**（6-2参照）。→ F / G に発展させた | — |
| **E. 管理側の可視化** | `送信ログ` シートから「今日まだ送信していない担当者」をスプレッドシート側で一覧化 | 高。ただし**発見型**（起きた後に気づく） | 端末側の対策ではない／管理者が毎日見る運用が前提／担当者マスタと突合する仕組みを別途作る必要がある | 小（**アプリ変更不要**。GAS/スプシ側のみ） |
| **F. バックグラウンド移行時に自動送信** | `visibilitychange`→`hidden` で未送信があれば `navigator.sendBeacon()`（またはiOS18なら `fetch(keepalive:true)`）で送る。ページ破棄後もブラウザが送信を完遂する | 中〜高。**スワイプ終了の直前を押さえられる唯一の手段**だが、**圏外区画では送れない**（ビーコンはリトライしない） | **圏外だと黙って失敗する**（Gでの後追いが前提）／応答を受け取れず `sent_flag=1` を確定できない／**GASの302リダイレクトを追えるか要実機検証**／本文64KB上限／画面ロックのたびに発火するので送信頻度が上がる | 中 |
| **G. 次回起動時の未送信検知** | `hidden` になった時刻と未送信件数をIndexedDBに記録し、次回起動時に**ブロッキングモーダル**で提示 | 高。**確実に本人の目に入る** | 事後対応でその場では止められない／毎回出ると無視されるので「未送信がある時だけ」に限定する必要がある | **小** |
| **H. アプリアイコンのバッジ** | `navigator.setAppBadge(未送信件数)` でホーム画面アイコンに件数表示 | 中〜高。**アプリを閉じた後に届く唯一の外部シグナル**。圏外でも機能する（表示だけなので通信不要） | **iOSバージョンの制約は解消**（18以上で要件クリア）。残る前提は「ホーム画面に追加（PWA）して使う運用にできるか」と通知許可／**PWAとSafariでストレージが分離される可能性があり、移行時に未送信データが引き継がれない恐れ**（要検証） | 小（ただしPWA運用の前提あり） |
| **I. 未送信表示の視覚強化** | ヘッダーバッジを件数に応じて赤く点滅、履歴タブに赤バッジ | 小 | アプリを見ていないと意味がない。単独では対策にならない | ほぼゼロ |

**組み合わせの相性**
- A と F は同じ「送ってしまう」系。**Aを入れるならFは取りこぼし専用**になるので、Fの実装は軽くて済む。
- B と C は「気づかせる」系で役割が重なる。**Bが発火しない運用（同一場所で一日中）ならCが効く**という補完関係。
- G は A〜F がすべて失敗した場合の最終防衛線。他とは競合しない。
- E だけ層が違う（管理側・発見型）。端末側の対策とは独立して併用できる。

#### 6-2. 「スワイプでアプリを終了する操作」は捕捉できるか（結論: **できない**）

アプリスイッチャーで上スワイプして落とす操作は**OSがプロセスを即座に終了させる**ため、
JavaScriptが動く余地がなく、**どのイベントも発火しない**。ネイティブアプリでも同じ。

ただし**スワイプで落とすには必ず先にアプリをバックグラウンドへ送る必要がある**。
ホームへ戻る／アプリスイッチャーを開く／画面ロック／別アプリへ切替 のいずれでも
`visibilitychange` が `hidden` で発火する。**これが捕まえられる最後のタイミング**。

その時点でアプリは見えていないので、**警告を出しても意味がない。できるのは「送ってしまう」こと**。

| イベント | iOS Safari での実情 |
|---|---|
| `beforeunload` | 発火が不安定。**確認ダイアログも出ない**（PCブラウザでは有効） |
| `unload` | 非推奨。iOSでは特に信頼できない |
| `pagehide` | 発火する。`visibilitychange` の補完に使える |
| `visibilitychange` → `hidden` | **確実に発火する。実質これが最後のチャンス** |

#### 6-3. 案F / 案H の実装メモ（着手時に読む）

**案F: sendBeacon の具体**
- 現行 `gasPost()` は `Content-Type: text/plain` で送っているので、
  `navigator.sendBeacon(GAS_ENDPOINT, new Blob([json], {type:'text/plain'}))` とすれば **GAS側は変更不要**。
- 応答が取れないため、`sent_flag` は 1 にせず「送信試行済み・未確認」の第3の状態を設け、
  次回起動時に `送信ログ` と突き合わせて確定させる設計にする。
- **最初に検証すべきは302リダイレクト**。GASウェブアプリは `googleusercontent.com` へ飛ぶ。
  検証は簡単で、バックグラウンドに送った後 `送信ログ` シートに行が増えるかを見るだけ。ここがNGなら案Fは不成立。
- 代替の `fetch(url, {keepalive:true})` はヘッダー制御も応答取得もできるが、Safariでの安定性は要検証。

**案H: バッジの前提**
- ホーム画面に追加（PWA）＋ iOS 16.4以降 ＋ 通知許可。
- 現場iPhoneのiOSバージョンは §6 の「判断待ちの情報」で未確認のまま。ここが確定しないと可否が決まらない。

#### 6-4. できないこと（検討済み・不可）

| やりたいこと | 可否 |
|---|---|
| スワイプ終了の直接検知 | **不可**（プロセス即終了でJSが動かない） |
| 終了時に確認ダイアログを出す | **不可**（iOSは `beforeunload` のダイアログを出さない） |
| Background Sync APIで通信復帰時に自動送信 | **不可**（Safari未サポート。Chrome/Edgeのみ） |
| 未送信があるときプッシュ通知 | 理論上可能だが、ホーム画面追加＋iOS16.4以降＋プッシュ配信サーバーが必要で重い |

#### 6-5. 推奨（6-0の前提条件を反映して改訂）

**圏外区画がある**という前提が入ったことで、「送ってしまう」系（A・F）だけでは閉じない。
**通信を必要としない対策（G・H・B・I）を土台に置き、その上に送信の自動化を乗せる**構成にする。

**推奨: G ＋ H ＋ B ＋ A（定期リトライ付き）**。FとEは余力に応じて。

| 順 | 案 | 理由 |
|---|---|---|
| 1 | **G. 次回起動時の未送信検知** | 通信不要・低コスト・確実に目に入る。圏外前提では**これが土台**。単独でも成立する |
| 2 | **H. アイコンバッジ** | iOS要件が解消して実用圏に入った。**通信不要**で、アプリを閉じた後に届く唯一のシグナル。圏外区画と相性が良い |
| 3 | **B. 場所/担当者の変更時に確認** | 圏外では機械的な送信が使えない。**人に促す**このタイプが相対的に最も効く。区画をまたぐ移動＝圏内へ戻る動きでもある |
| 4 | **A. 自動送信（定期リトライ付き）** | 圏内にいる間は自動で片付く。ただし下記の設計が必須 |
| 5 | F / E | Fは圏外だと黙って失敗するのでGが前提。Eは管理側の別種の担保として併用可 |

**案Aを圏外前提で設計する際の必須事項**
- `navigator.onLine` は**APに繋がってさえいれば疎通できなくても `true`** を返す。
  工場の弱電界では「オンラインなのに送信失敗」が普通に起きるため、
  `online` イベントだけに頼らず**一定間隔のリトライ**を必ず併用する。
- 失敗しても `sent_flag` は 0 のまま。既存の `gasWithRetry`（3回・指数バックオフ）は
  短時間の瞬断向けなので、圏外区画からの復帰にはより長い間隔の再試行が要る。
- 送信の重なりを防ぐ排他制御（送信中フラグ）を入れる。

**あわせて入れたいUI（案Iの一部）**
- **オフライン状態の明示**。いま送信できない状況であることが分かれば、
  作業者が「圏内に戻ってから送る」と判断できる。現状はオフラインでも見た目が変わらない。

**②との関係**: 「重複登録OK・数量の訂正は修正機能で行う」運用にしたので、
自動送信の副作用（誤データがすぐシートに入る）は許容できる。以前は「送信＝確定」の重みがあったが、
いまは修正が効くので自動化しやすくなっている。

### ⑦ IBM i連携CSV（**別途対応。未着手**）

⑥とは別タスクとして切り出し。着手前にIBM i側（情シス／基幹担当）に確認すべき事項。

#### 7-1. 位置づけ・運用
- [ ] 現行 `スプシ→ASTERIA→IBM i` の**代替**（ASTERIA障害時の手動投入）か、**並行**か、**置き換え**か
- [ ] 誰がいつ実行するか（現場担当者が端末から / 管理者がまとめて）
- [ ] 出力単位（1回の送信分 / 1日分 / 棚卸期間全体 / 場所・担当者ごと）
- [ ] `棚卸データ` と `棚卸データ(特注品)` を**同一ファイル**にするか**別ファイル**にするか
      （同一にする場合、特注品を区別するフラグ列が要る）

#### 7-2. ファイル形式
- [ ] 文字コード: Shift-JIS(CP932) / IBM漢字(CP930,CP939) / UTF-8 / UTF-8 BOM付き
- [ ] 改行コード: CRLF / LF / なし（固定長でレコード長区切り）
- [ ] レコード形式: **カンマ区切り** / タブ区切り / **固定長**（← 基幹取込は固定長が多い）
- [ ] 囲み文字: なし / ダブルクォート（全項目 or 区切り文字を含む項目のみ）
- [ ] ヘッダー行: あり / なし
- [ ] 最終行の改行: あり / なし
- [ ] ファイル名の規則（例 `TANA_YYYYMMDD_<場所コード>_<連番>.csv`）と拡張子

#### 7-3. レコードレイアウト（**最重要**）
アプリが持っている項目と、IBM i側に必要な定義の対応表。項目ごとに
**出力有無／順序／フィールド名／型(CHAR/ZONED/PACKED/DEC)／桁数（バイトか文字か）／
詰め方向と埋め文字** を確認する。

| アプリの項目 | 現在の中身 | 確認事項 |
|---|---|---|
| 日付 | `2026/08/26`（文字列） | 出力形式。`YYYYMMDD` か区切り付きか |
| 時刻 | `13:45:02`（文字列） | 出力要否。`HHMMSS` か |
| 図番 | 英数大文字・**ハイフン除去済み** | IBM i側もハイフン無しでよいか。桁数と詰め方向 |
| 製番 | 数字9桁・**任意（空あり）** | 空のときスペースかゼロ埋めか |
| 品名 | 全角含む。パディング除去済み | 桁数（Shift-JISなら全角2バイト）。**溢れた場合は切り捨てか** |
| サイズ | 全角含む。**空が多い**（実測40,573件が空） | 同上 |
| 長さ | mm整数。**対象外部材は空文字** | 空のとき スペース / `0` / ゼロ埋め のどれか |
| 数量 | 整数（`parseInt`） | 小数の要否、桁数、ゼロ埋め、符号の持ち方 |
| 棚卸部署 | **名称**（例「第一製造部」） | → 7-4参照 |
| 担当者 | **名称**（例「田嶋 一郎」） | → 7-4参照 |
| 在庫区分 | 名称（製品/仕掛品/材料） | コード値が必要か |

**アプリが持っていない項目**（必要なら改修が要る）:
- **単位**: IndexedDBのレコードには `unit` があるが、**スプレッドシートには書いていない**。
  CSVに要るなら、シートに列を足すかCSV生成時にマスタから引き直す必要がある。
- **棚卸年月 / 棚卸区分 / 棚卸コード**: 基幹の棚卸ファイルはこれらをキーに持つことが多い。
  必要ならアプリ側で入力・保持する仕組みから作ることになる。
- **入力者ID / 端末ID**: 監査用に要求されることがある。

#### 7-4. コード変換（**設計インパクトが大きい**）
`プルダウンマスタ` には **B列=部門コード** と **E列=担当者コード** があるが、
**現在のアプリはどちらも使っておらず、名称のみを保持している**。
IBM i側がコード値を要求する場合、以下の改修が必要になる。
- `getAllPersonnel` / `getDropdowns` を拡張してコードも返す
- 端末側で名称とコードを対応付けて保持する
- 既存の棚卸データ（名称のみ）を後からコードに引き当てる方法を決める

→ **CSVの項目にコードが含まれるかどうかを最初に確認すること。** ここで方針が変わる。

- [ ] 棚卸部署は 名称 / 部門コード(B列) のどちらで出すか
- [ ] 担当者は 名称 / 担当者コード(E列) のどちらで出すか
- [ ] 在庫区分にコード体系はあるか（製品→1 等）

#### 7-5. ヘッダ/トレーラレコード
- [ ] ヘッダレコード（棚卸年月・場所・作成日時など）が必要か
- [ ] トレーラレコード（件数・数量合計）が必要か。あればその桁数

#### 7-6. 受け渡し方法
- [ ] アプリから端末にダウンロード / GASでDriveに保存 / メール添付 / 共有フォルダ
- 制約: **iPhoneのSafariではダウンロードが「ファイル」アプリ保存になる**。
  そこからIBM i側へ渡す導線が現場で成立するかを確認すること。
  管理者がPCで一括出力する運用なら、GASでDrive保存の方が現実的。

#### 7-7. 二重投入・再実行
- [ ] 同じデータを2回流したときIBM i側はどうなるか（キー重複エラー / 上書き / 二重計上）
- [ ] 出力し直しが必要になった場合の手順

#### 7-8. こちらから提示しておく技術的制約
- **Shift-JISが必要な場合、GAS側で生成する方が圧倒的に簡単。**
  GASは `blob.setDataFromString(str, 'Shift_JIS')` で変換できるが、
  ブラウザの `TextEncoder` は**UTF-8しか出せない**ため、端末側で作るなら
  encoding.js等の外部ライブラリ（約50KB）をCDNから追加する必要がある。
- **Shift-JISに変換できない文字**（機種依存文字、①、㈱、〜 など）が品名に混ざる可能性がある。
  その場合の扱い（`?` に落とす / エラーで止める / 事前にマスタを直す）を決めておくこと。
- 固定長の場合、**桁数はバイト数か文字数か**を必ず確認する。
  品名・サイズは全角を含むため、Shift-JISでは全角1文字=2バイトになり計算が変わる。

### 最優先の技術的残課題: GAS側マスタ7.2MBがキャッシュ不可
OCR照合のたびに `getMasterMap()` がシート70,200行を読み込み、さらに `searchPart()` が未ヒット時に**全件を最大3周**（類似文字補正×2＋編集距離1）。OCRの応答が数秒〜十数秒かかる原因。

**案C: gzip転送**
- 効果: 起動時転送 8.79MB → 約0.63MB（93%減）
- **OCRの遅さには一切効かない**
- リスク: `DecompressionStream` は **iOS 16.4以降**。古い端末が混ざると不可（pako 45KB をCDN併用で回避可能）。GAS側で6.5MB文字列のgzip+Base64化が実行時間・メモリ上限に触れる可能性。
- v3.8.0のTTL活用で起動時通信が1日1回に減ったため、**費用対効果は相対的に下がった**。ただしマスタ増加時のレスポンスサイズ上限対策としては有効。

**案D: あいまい検索のフロント移管（本命）**
- 効果: GASのシート70k読込＋全件走査が消える。フロントは既に全マスタをメモリ保持しているのでデータ的な追加コストはゼロ。事前索引（正規化済みキーのMap）を作れば全件走査O(n)→ハッシュO(1)で元実装より速い。
- **注意: OCR自体(Vision API)はネットワーク必須。オフラインになるのは照合部分のみ**（過去に説明を訂正済み）。
- リスク: **挙動の同一性**。`normalizeForMatch` の類似文字テーブル、ハイフン揺れ4パターン、編集距離1の判定順序を完全移植しないと、今まで当たっていた図番が当たらなくなる。棚卸の正確性に直結。
- 推奨: 着手前に**実績626件で新旧突合するテスト**を用意する。
- 起動時の8.79MBは残る。

### その他の残課題
- **GAS側 LockService 未導入**: registerBatch / updateItem / registerInventory の書き込みが直列化されていない。v3.6.0の②で重複行リスクは大きく下げたが、根本対策としてスクリプトロックを入れる余地あり。
- **v3.4.0以前の旧レコード**: `scan_src` 未定義のためJAN/QR扱いになりOCR図番修正ができない。後方互換の割り切りとして許容中。

### 判断待ちの情報

**回答済み（2026-08-26）**
- ~~現場iPhoneのiOSバージョン~~ → **最低 iOS 18、近々 iOS 26 へ**。
  §5の案C（gzip転送）の `DecompressionStream` 要件（iOS 16.4以降）は**クリア済み**。
  ⑥の案H（アイコンバッジ）も要件を満たす。
- ~~工場内の電波状況~~ → **WiFiの届かない区画がある**。⑥の推奨構成を 6-5 で改訂済み。

**未確認**
- **現場端末はWiFi専用か、セルラー（SIM）も入っているか** — ⑥の案Aの効果を大きく左右する。
  セルラーがあれば圏外区画でもLTEで送れるため、自動送信がそのまま効く。
- ホーム画面に追加（PWA）して使う運用にできるか — ⑥の案Hの前提。
  なお標準の `apple-mobile-web-app-capable` メタは既に入っているので、追加自体は今でも可能。
- マスタ件数の今後の増加見込み（§6の案C・gzip転送の優先度を左右）

---

## 7. デプロイ・検証手順

**Code.gs**: 変更時はウェブアプリの**新バージョン発行が必要**。反映後 `diagTest()` を実行し、キャッシュサイズ診断と担当者マップを確認。

**index.html**: GitHub Pagesに配置するだけ。v3.7.0以降は初回起動時にIndexedDBがv2へスキーマ更新（既存履歴は保持）。

**v4.0.0 は Code.gs も index.html も変更あり。GASのウェブアプリを新バージョンで再デプロイすること。**
先に「棚卸データ」シートのG列に「長さ」が入っていることを確認する（入っていないと部署・担当者が1列ずれて書き込まれる）。

**主要な動作確認**
| 対象 | 手順 |
|---|---|
| v4.0.0 ② 重複登録 | 同じ図番を2回スキャンして登録→一括送信→棚卸データに**2行**できるか |
| v4.0.0 ② 再送の冪等性 | 送信済みを履歴で修正→再送信→行が**増えず上書き**されるか |
| v4.0.0 ③ 長さ | Ｈ型鋼をスキャン→長さ欄が出るか／ボルト等では出ないか／G列に入るか |
| v4.0.0 ③ 長さの持ち越し | 長さ入力後に別の対象外部材をスキャン→長さ欄が消え値も残らないか |
| v4.0.0 ③ 長さ必須 | 長さ未入力のまま登録ボタン→押せない／数量欄でEnter→警告が出て登録されないか |
| v4.0.0 ③ 旧データの穴埋め | v4.0.0以前のＨ型鋼レコードを履歴から開く→長さ入力を求められるか（キャンセルは可） |
| v4.0.0 ④ 特注品 | K/P始まりでマスタ未登録の図番→履歴に「特注」タグ→`棚卸データ(特注品)`へ入るか |
| v4.0.0 ④ シート間移動 | 特注品を送信後、履歴で図番をマスタ登録品に修正→通常シートへ移動し特注品シートから消えるか |
| v4.0.0 ⑤ QR ROI | QRが2つ並んだ紙面で、枠に入れた方だけが読まれるか。枠外のQRが読まれないか |
| v4.0.0 ⑤ ズーム併用 | デジタルズーム中でも枠内のQRが読めるか（枠外を切り出していないか） |
| v4.1.0 末尾0 | マスタ未登録のコードをスキャン→図番が**切り詰められず原文のまま**入るか |
| v4.1.0 末尾0 | 末尾0リストに載っている図番は従来どおり正しく確定するか（デグレ確認） |
| v3.8.0 TTL | 一度マスタ読込→アプリを閉じて再度開く。即起動＋「保存済み・○分前」トーストが出ればOK |
| v3.7.0 キャッシュ | 設定タブ「キャッシュ状態を確認」→「保存済み ✔ マスタ 70,200件」 |
| v3.7.0 オフライン | 機内モードで再起動→「保存済みマスタで起動しました」 |
| v3.6.0 ズーム | HWズーム非対応端末で `(D)` 表示にし、薄いバーコードが従来より読めるか |
| v3.6.0 二重書込 | 未送信のまま修正→一括送信→棚卸データシートに**1行だけ**できるか |
| v3.6.0 品名再照合 | OCR登録→履歴タップ→図番だけ変えて**検索せず**保存→品名が追従するか |
| v3.5.0 重複キー | 製番空のJAN/QRで同じ図番を2回スキャン→一括送信でエラーが出ないか |

---

## 8. 作業上の申し送り

- **コード全文を記憶から再構成しない**。index.htmlは約3,150行あり、v3.6.0の作業中に全文再生成を試みて中断した経緯がある。既存ファイルへの外科的パッチ（str_replace）を基本とする。
- 変更後は必ず検証すること: インラインJSを抽出して `node --check`、HTML構造タグ数、`getElementById` 参照IDが全て存在するか、関数定義の重複なし。
- コード内の `★ [vX.Y.Z]` コメントは修正理由の記録として機能している。**なぜそう直したか**を残す方針を継続。
- 過去の該当チャット: `380601a3-84c8-4283-a5f7-fec180f39875`, `ee237d79-e781-4fe3-8cce-244eca306e46`
- v4.0.0 の作業では **Pythonの厳密一致パッチスクリプト**で外科的に当てた（見つからなければ即エラーで止まる方式）。
  作業前のファイルは `Code.gs.bak` / `index.html.bak` として同じフォルダに残してある。
- **Node は導入済み**（v24.19.0 / `C:\Program Files\nodejs`。Git Bashからは `export PATH="$PATH:/c/Program Files/nodejs"` が必要）。
  検証は次の2段構え:
  1. `index.html` からインラインJSを抽出して `node --check`（Code.gs はそのままコピーして `node --check`）
  2. 独自スクリプトで「関数定義の重複」「`getElementById`の参照IDが全て実在」「インラインハンドラの関数実在」
     「id属性の重複」「divタグ数」を検査
  抽出スクリプトは先頭を空行で埋めて index.html と行番号が一致するようにしてある（エラー箇所がそのまま追える）。
  なお `node --check` は構文しか見ない。実機・実スプシでの動作確認は §7 の表で別途行うこと。
